// @ts-check
import { defineConfig } from "astro/config";

// https://astro.build/config
export default defineConfig({
  // GitHub
  // site: "https://alvar-s.github.io",
  // base: "Atk",

  // Palvelin
  site: "https://nettisivukoti.fi",
});
